#! /usr/bin/env plackup
# -*- cperl -*-
use strict;
use warnings;

use lib 'lib'; # ugh, but my plackup works
my $lib = $ENV{PERL5LIB};
%ENV = (); # don't need all that
$ENV{PERL5LIB} = "lib:$lib"; # double ugh, but cgi subprocess works

use Test::HTtapTP::App;

my $app = Test::HTtapTP::App->new->to_app;
